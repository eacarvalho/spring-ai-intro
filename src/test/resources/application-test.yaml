spring:
  ai:
    chat:
      client:
        enabled: true
    ollama:
      chat:
        options:
          temperature: 0.3
    vectorstore:
      type: none

sfg:
  aiapp:
    vector-store-local: true
    vector-store-enabled: false